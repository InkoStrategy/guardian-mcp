{
  "name": "guardian-mcp",
  "version": "1.6.0",
  "description": "Guardian MCP - deterministic security verdicts (ALLOW/WARN/DENY) for EVM transactions. A2MCP service for OKX.AI with optional x402 payments.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "safe-pay": "node scripts/safe-pay.js",
    "trust-scan": "node scripts/okxai-trust-scan.js",
    "verify-settlement": "node scripts/verify-settlement.js"
  },
  "dependencies": {
    "@okxweb3/x402-core": "^0.1.0",
    "@okxweb3/x402-evm": "^0.2.1",
    "ethers": "^6.13.4"
  }
}
